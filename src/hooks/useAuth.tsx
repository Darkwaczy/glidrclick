// This file is no longer needed as we're using AuthContext directly
// Keeping this as a simple re-export to avoid breaking any existing imports
export { useAuthContext as useAuth } from '@/context/AuthContext';
